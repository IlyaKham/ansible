# Vector configuration for CentOS 7
# Deployed by Ansible on {{ ansible_date_time.iso8601 }}

[sources.demo_logs]
type = "demo_logs"
format = "json"
lines = [
  '{"message": "Hello from Vector", "host": "{{ ansible_hostname }}"}',
  '{"message": "This is a test log", "level": "info"}',
  '{"message": "Vector is running", "level": "debug"}',
]

[sinks.console]
type = "console"
inputs = ["demo_logs"]
target = "stdout"
encoding.codec = "text"

[sinks.file]
type = "file"
inputs = ["demo_logs"]
path = "/var/log/vector/vector.log"
encoding.codec = "text"
